# secrets.example.yaml — TEMPLATE for sops-nix.
# Copy to secrets/secrets.yaml, fill in, and `sops -e -i secrets/secrets.yaml`
# to encrypt. Reference these in squad-fleet.nix via config.sops.secrets.<name>.path
# instead of the inline CHANGE_ME_* placeholders. NEVER commit the decrypted file.
rcon_password: "your-rcon-password-here"
discord_bot_token: "your-discord-bot-token-here"
